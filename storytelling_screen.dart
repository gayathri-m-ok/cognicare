import 'package:flutter/material.dart';

class StorytellingScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Storytelling')),
      body: Center(child: Text('Storytelling Section')),
    );
  }
}
